import {supabase} from './supabase-client.js';
export async function getAgentDashboard(){const {data:{user}}=await supabase.auth.getUser();if(!user)throw Error('Please sign in first.');const [{data:listings,error:lerr},{data:profile,error:perr}]=await Promise.all([supabase.from('listings').select('id,title,status,property_type,listing_purpose,city,state').eq('seller_id',user.id).eq('category','Real Estate & Properties').order('created_at',{ascending:false}),supabase.from('seller_profiles').select('verification_status').eq('user_id',user.id).maybeSingle()]);if(lerr)throw lerr;if(perr)throw perr;return {listings:listings||[],verification_status:profile?.verification_status||'unverified'};}
